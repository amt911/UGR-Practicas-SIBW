{% extends "formulario_plantilla.twig" %}

{% block archivosAdicionales %}
{{ parent() }}
<link rel="stylesheet" href="static/css/lista_comentarios.css">
{% endblock %}

{% block main %}
{{ parent() }}

<div class="rojo">
    {% for aux in Errores %}
        <h2>{{ aux }}</h2>
    {% endfor %}
</div>
{% if ShowForm == true %}
<form action="lista_comentarios.php" method="POST">
    <input type="text" name="keyword" placeholder="Buscar..."/>
    <button>Enviar</button>
</form>

{#
<table>
    <tr>
        <td>ID</td>
        <td>Nombre</td>
        <td>Email</td>
        <td>Comentario</td>
        <td>Fecha</td>
    </tr>
    #}

    
    <div id="comentarios-submit-container">
    {% for aux in Comentarios %}
    {#
        <tr>
            <td>{{ aux.ID }}</td>
            <td>{{ aux.Nombre }}</td>
            <td>{{ aux.Correo }}</td>
            <td>{{ aux.Texto }}</td>
            <td>{{ aux.Fecha }}</td>
            <td>
                <a href="edit.php?back=lista_comentarios.php&id={{ aux.ID }}">Editar</a>
            </td>            
            <td>
                <a href="delete.php?back=lista_comentarios.php&id={{ aux.ID }}">Borrar</a>
            </td>
        </tr>
        #}

        <div class="comentario-container">
            <div class="mod-comentario-container">
                <div class="editar">
                    <a href="edit.php?back={{ Back }}&id={{ aux["ID"] }}">
                        <img class="imagen ref-editar" src="static/images/edit.png" alt="editar"/>
                    </a>
                </div>
                <div class="borrar">
                    <a href="delete.php?back={{ Back }}&id={{ aux["ID"] }}">
                        <img class="imagen ref-borrar" src="static/images/delete.png" alt="borrar" onclick="return confirmacion();"/>
                    </a>
                </div>
            </div>

            <div class="nombre-comentario">
                <h4>{{ aux["Nombre"] }}</h4>
            </div>
            <div class="comentario-email">
                {{ aux["Correo"] }}
            </div>
            <div class="fecha">
                {{ aux["Fecha"] }}<!--12 de mayo del 2021, 13:37-->
            </div>
            <div class="contenido-comentario">
                {{ aux["Texto"] }}
            </div>
        </div>   
    {% endfor %}
    </div>
{#</table>#}
{% endif %}


{% endblock %}